import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.161.0/build/three.module.js';
const c=document.querySelector('#game'),stage=c.parentElement,scoreEl=document.querySelector('#score'),speedEl=document.querySelector('#speed'),bestEl=document.querySelector('#best'),start=document.querySelector('#start'),over=document.querySelector('#over'),finalEl=document.querySelector('#final');let r,s,cam,timer=new THREE.Clock(),player,traffic=[],playing=false,score=0,target=0,spawn=0,best=+localStorage.getItem('gece3d-best')||0;bestEl.textContent=String(best).padStart(6,'0');const lanes=[-3.25,0,3.25],M=x=>new THREE.MeshStandardMaterial({color:x,roughness:.9}),B=(w,h,d,x)=>new THREE.Mesh(new THREE.BoxGeometry(w,h,d),M(x));
function car(x){let g=new THREE.Group(),b=B(2.15,.65,4,x);b.position.y=.65;g.add(b);let top=B(1.55,.6,1.8,0x344250);top.position.set(0,1.06,-.2);g.add(top);for(let z of[-1.55,1.55])for(let x of[-.95,.95]){let q=B(.2,.4,.55,0x171b20);q.position.set(x,.4,z);g.add(q)}return g}
function build(){s=new THREE.Scene();s.background=new THREE.Color(0x1a252e);s.fog=new THREE.Fog(0x1a252e,42,165);cam=new THREE.PerspectiveCamera(62,1,.1,280);cam.position.set(0,5.2,11);r=new THREE.WebGLRenderer({canvas:c,antialias:true});r.setPixelRatio(Math.min(devicePixelRatio,2));r.setSize(stage.clientWidth,stage.clientHeight,false);s.add(new THREE.HemisphereLight(0xc3d5dc,0x34414a,2.2));let sun=new THREE.DirectionalLight(0xffe4c4,1.4);sun.position.set(-30,40,20);s.add(sun);let g=new THREE.Group();s.add(g);let ground=B(180,.2,260,0x273139);ground.position.set(0,-.2,-90);g.add(ground);let road=B(14,.1,260,0x3a4248);road.position.z=-90;g.add(road);for(let x of[-2.15,2.15])for(let z=0;z>-220;z-=18){let q=B(.12,.03,6,0xb8a875);q.position.set(x,.02,z);g.add(q)}for(let side of[-1,1])for(let i=0;i<12;i++){let z=-8-i*16,h=5+(i*7)%9,w=5+(i*3)%4,q=B(w,h,8,side<0?0x34434d:0x3d4c55);q.position.set(side*(12+(i*3)%6),h/2,z);g.add(q)}player=car(0x72c9c8);player.position.set(0,0,4);s.add(player);resize()}
function resize(){if(!r)return;let w=stage.clientWidth,h=stage.clientHeight;cam.aspect=w/h;cam.updateProjectionMatrix();r.setSize(w,h,false)}addEventListener('resize',resize);function add(){let a=car([0xc97968,0xd5ad62,0x8196ac,0x9b8eb6][Math.floor(Math.random()*4)]);a.position.set(lanes[Math.floor(Math.random()*3)],0,-115);a.userData.lane=Math.round((a.position.x+3.25)/3.25);traffic.push(a);s.add(a)}function finish(){playing=false;let n=Math.floor(score);finalEl.textContent=n;best=Math.max(best,n);localStorage.setItem('gece3d-best',best);bestEl.textContent=String(best).padStart(6,'0');over.classList.remove('gone')}function begin(){playing=true;score=0;spawn=0;target=0;traffic.forEach(a=>s.remove(a));traffic=[];start.classList.add('gone');over.classList.add('gone');timer.start()}function move(d){if(playing)target=Math.max(-1,Math.min(1,target+d))}function loop(){requestAnimationFrame(loop);let dt=Math.min(timer.getDelta(),.04);if(playing){score+=dt*13;let v=18+score*.02;player.position.x=THREE.MathUtils.lerp(player.position.x,lanes[target+1],dt*7);spawn-=dt;if(spawn<=0){add();spawn=Math.max(.55,1.4-score/1800)}traffic.forEach(a=>a.position.z+=v*dt);traffic=traffic.filter(a=>{if(a.position.z>14){s.remove(a);return false}return true});if(traffic.some(a=>a.userData.lane===target+1&&Math.abs(a.position.z-player.position.z)<3.4))finish();scoreEl.textContent=String(Math.floor(score)).padStart(6,'0');speedEl.textContent=String(Math.min(170,86+Math.floor(score/25))).padStart(3,'0');cam.position.x+=(player.position.x*.15-cam.position.x)*dt*4;cam.lookAt(player.position.x*.15,1,-22)}r.render(s,cam)}document.querySelector('#startBtn').onclick=begin;document.querySelector('#again').onclick=begin;addEventListener('keydown',e=>{if(e.key==='ArrowLeft'||e.key.toLowerCase()==='a')move(-1);if(e.key==='ArrowRight'||e.key.toLowerCase()==='d')move(1);if(e.key===' '&&!playing)begin()});let tx=0;c.addEventListener('touchstart',e=>tx=e.touches[0].clientX,{passive:true});c.addEventListener('touchend',e=>{let d=e.changedTouches[0].clientX-tx;if(Math.abs(d)>20)move(d>0?1:-1)},{passive:true});build();loop();
